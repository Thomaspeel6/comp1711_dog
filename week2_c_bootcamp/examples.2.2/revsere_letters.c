#include <stdio.h>

int main(){

    char  str[] = 'Hello' ;
    int a;

    while (str[a] ! = '\0'){
        a++
    }
    for (int i = a ; i >= 0, a--){
       if (str[i] ==  '\0'){
        printf('//0');
       }
       e {
            printf("%c ", str[i]);
        }
    }
    printf('\n')
    return (0);
}